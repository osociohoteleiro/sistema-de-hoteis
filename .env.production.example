# =================================================================
# EXEMPLO DE VARIÁVEIS DE AMBIENTE PARA PRODUÇÃO - EASYPANEL
# =================================================================
# 
# INSTRUÇÕES:
# 1. Copie este arquivo e renomeie para .env.production
# 2. Preencha todas as variáveis com valores reais
# 3. Configure no EasyPanel através da aba "Environment" do serviço
# 4. NUNCA commite este arquivo com valores reais no Git
#
# =================================================================

# =================================================================
# CONFIGURAÇÕES BÁSICAS
# =================================================================
NODE_ENV=production
PORT=3001

# =================================================================
# BANCO DE DADOS POSTGRESQL (EASYPANEL)
# =================================================================
# Use as variáveis mágicas do EasyPanel quando configurar
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=$(PROJECT_NAME)_user
POSTGRES_PASSWORD=$(SERVICE_SECRET)
POSTGRES_DB=$(PROJECT_NAME)_db

# Configuração da Database URL completa (EasyPanel pode gerar automaticamente)
DATABASE_URL=postgresql://$(PROJECT_NAME)_user:$(SERVICE_SECRET)@postgres:5432/$(PROJECT_NAME)_db

# =================================================================
# REDIS CACHE (EASYPANEL)
# =================================================================
REDIS_HOST=redis
REDIS_PORT=6379
# REDIS_PASSWORD=seu_redis_password

# =================================================================
# JWT SEGURANÇA
# =================================================================
# IMPORTANTE: Gere uma chave segura em produção
JWT_SECRET=your-super-secure-production-jwt-secret-key-here-minimum-32-characters
JWT_EXPIRES_IN=24h

# =================================================================
# CORS - DOMÍNIOS PERMITIDOS
# =================================================================
# Substitua pelos seus domínios reais
CORS_ORIGIN=https://app.seu-dominio.com,https://pms.seu-dominio.com

# =================================================================
# CACHE TTL (EM SEGUNDOS)
# =================================================================
CACHE_TTL_DEFAULT=3600
CACHE_TTL_USER=1800
CACHE_TTL_HOTEL=7200

# =================================================================
# POOL DE CONEXÕES POSTGRESQL
# =================================================================
PG_POOL_MIN=5
PG_POOL_MAX=20
PG_POOL_IDLE_TIMEOUT=30000
PG_POOL_CONNECTION_TIMEOUT=10000

# =================================================================
# AWS S3 STORAGE (OPCIONAL)
# =================================================================
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_REGION=us-east-1
AWS_S3_BUCKET=hotel-osh-production

# =================================================================
# WHATSAPP EVOLUTION API (OPCIONAL)
# =================================================================
EVOLUTION_API_URL=https://your-evolution-api.com
EVOLUTION_API_KEY=your_evolution_api_key

# =================================================================
# FLOWISE AI (OPCIONAL)
# =================================================================
FLOWISE_API_URL=https://your-flowise-instance.com
FLOWISE_API_KEY=your_flowise_api_key

# =================================================================
# QDRANT VECTOR DATABASE (OPCIONAL)
# =================================================================
QDRANT_URL=https://your-qdrant-instance.com
QDRANT_API_KEY=your_qdrant_api_key

# =================================================================
# LOGS E MONITORAMENTO
# =================================================================
LOG_LEVEL=info
LOG_FILE=/app/logs/app.log

# =================================================================
# RATE LIMITING
# =================================================================
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# =================================================================
# SESSION E SECURITY
# =================================================================
SESSION_SECRET=your-super-secure-session-secret-here-minimum-32-characters
BCRYPT_SALT_ROUNDS=12

# =================================================================
# EMAIL (OPCIONAL - PARA NOTIFICAÇÕES)
# =================================================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# =================================================================
# MONITORAMENTO E ANALYTICS (OPCIONAL)
# =================================================================
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
GOOGLE_ANALYTICS_ID=UA-XXXXXXXXX-X

# =================================================================
# CONFIGURAÇÕES ESPECÍFICAS DO HOTEL
# =================================================================
DEFAULT_CURRENCY=BRL
DEFAULT_LOCALE=pt-BR
DEFAULT_TIMEZONE=America/Sao_Paulo

# =================================================================
# BACKUP E RECOVERY (OPCIONAL)
# =================================================================
BACKUP_SCHEDULE="0 2 * * *"
BACKUP_RETENTION_DAYS=30

# =================================================================
# DOMÍNIOS E URLs (FRONTEND)
# =================================================================
FRONTEND_URL=https://app.seu-dominio.com
API_URL=https://api.seu-dominio.com
PMS_URL=https://pms.seu-dominio.com

# =================================================================
# WEBHOOK URLs (OPCIONAL)
# =================================================================
WEBHOOK_URL=https://api.seu-dominio.com/webhooks
PAYMENT_WEBHOOK_SECRET=your_payment_webhook_secret

# =================================================================
# FEATURE FLAGS (OPCIONAL)
# =================================================================
ENABLE_RATE_SHOPPER=true
ENABLE_AUTOMATION=true
ENABLE_ANALYTICS=true
ENABLE_BACKUP=true