# ==============================================
# CONFIGURAÇÕES DE PRODUÇÃO - OSH SYSTEM
# ==============================================
# IMPORTANTE: Altere TODOS os valores padrão antes do deploy!

# PostgreSQL Database (Produção)
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=osh_user
POSTGRES_PASSWORD=CHANGE_THIS_SECURE_PASSWORD_2024!
POSTGRES_DB=osh_db_prod

# Redis Cache
REDIS_HOST=redis
REDIS_PORT=6379
# REDIS_PASSWORD=CHANGE_THIS_REDIS_PASSWORD_2024!

# Database Mode
DB_MODE=postgres

# Configurações do Servidor
PORT=3001
NODE_ENV=production

# JWT Secret (ALTERE OBRIGATORIAMENTE!)
JWT_SECRET=CHANGE_THIS_TO_A_VERY_SECURE_JWT_SECRET_KEY_FOR_PRODUCTION_2024!

# CORS (Configure com seu domínio)
CORS_ORIGIN=https://seu-dominio.com,https://pms.seu-dominio.com,https://automacao.seu-dominio.com

# Cache TTL (em segundos)
CACHE_TTL_DEFAULT=7200
CACHE_TTL_USER=3600
CACHE_TTL_HOTEL=14400

# Pool de Conexões PostgreSQL (Otimizado para produção)
PG_POOL_MIN=10
PG_POOL_MAX=100
PG_POOL_IDLE_TIMEOUT=30000
PG_POOL_CONNECTION_TIMEOUT=10000

# Meta/Facebook Ads Configuration (Configure com suas credenciais)
META_APP_ID=SEU_META_APP_ID
META_APP_SECRET=SEU_META_APP_SECRET
META_API_VERSION=v18.0
META_REDIRECT_URI=https://api.seu-dominio.com/api/meta/oauth/callback

# AWS S3 Configuration (Configure se usar S3)
# AWS_ACCESS_KEY_ID=SEU_AWS_ACCESS_KEY
# AWS_SECRET_ACCESS_KEY=SEU_AWS_SECRET_ACCESS_KEY
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=seu-bucket-osh

# Email Configuration (Configure se usar email)
# SMTP_HOST=smtp.seu-provedor.com
# SMTP_PORT=587
# SMTP_USER=seu-email@dominio.com
# SMTP_PASS=sua-senha-email

# Rate Limiting (Produção mais restritiva)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# Logging
LOG_LEVEL=info
LOG_FILE=/app/logs/app.log

# Health Check
HEALTH_CHECK_ENABLED=true

# Backup Configuration
BACKUP_ENABLED=true
BACKUP_FREQUENCY=daily
BACKUP_RETENTION_DAYS=30

# ==============================================
# INSTRUÇÕES IMPORTANTES:
# ==============================================
# 1. ALTERE todas as senhas padrão
# 2. Configure CORS_ORIGIN com seu domínio real
# 3. Configure META_* com credenciais reais do Facebook
# 4. Configure AWS_* se usar S3 para uploads
# 5. Configure SMTP_* se usar funcionalidade de email
# 6. Mantenha este arquivo seguro e nunca commite no Git!