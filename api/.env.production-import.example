# Configuração para importação de dados para PRODUÇÃO
# Copie este arquivo para .env.production-import e configure os valores

# ====================================
# BANCO LOCAL (FONTE DOS DADOS)
# ====================================

# PostgreSQL Local (onde estão seus dados atuais)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=osh_user
POSTGRES_PASSWORD=osh_password_2024
POSTGRES_DB=osh_db

# ====================================
# BANCO DE PRODUÇÃO (DESTINO)
# ====================================

# PostgreSQL Produção (EasyPanel)
# IMPORTANTE: Configure estes valores com as credenciais do seu banco de produção
PROD_POSTGRES_HOST=your-easypanel-postgres-host
PROD_POSTGRES_PORT=5432
PROD_POSTGRES_USER=hotel_user
PROD_POSTGRES_PASSWORD=SUA_SENHA_DE_PRODUCAO_AQUI
PROD_POSTGRES_DB=hotel_osh_db

# SSL para conexão de produção (geralmente true no EasyPanel)
PROD_POSTGRES_SSL=true

# ====================================
# EXEMPLO PARA EASYPANEL
# ====================================

# Se você está usando EasyPanel, suas configurações serão algo como:
# PROD_POSTGRES_HOST=postgres (nome do serviço PostgreSQL no EasyPanel)
# PROD_POSTGRES_PORT=5432
# PROD_POSTGRES_USER=hotel_user (usuário configurado no EasyPanel)
# PROD_POSTGRES_PASSWORD=sua_senha_configurada_no_easypanel
# PROD_POSTGRES_DB=hotel_osh_db (nome do banco configurado no EasyPanel)
# PROD_POSTGRES_SSL=false (para conexões internas no EasyPanel)

# ====================================
# PARA CONEXÃO EXTERNA AO EASYPANEL
# ====================================

# Se você quiser conectar de fora do EasyPanel:
# PROD_POSTGRES_HOST=seu-dominio-easypanel.com (ou IP do servidor)
# PROD_POSTGRES_PORT=5432 (ou porta exposta)
# PROD_POSTGRES_SSL=true (recomendado para conexões externas)

# ====================================
# COMO USAR ESTE ARQUIVO
# ====================================

# 1. Copie este arquivo: cp .env.production-import.example .env.production-import
# 2. Configure os valores de PROD_* com suas credenciais de produção
# 3. Execute o script: node import-production-data.js
# 4. O script usará as configurações locais para buscar dados e as de produção para inserir